"use strict";
// import { Router } from "express";
// import {
//   acceptRequest,
//   cancelRequest,
//   getOutgoingRequests,
//   rejectRequest,
//   sendRequest,
//   viewIncoming,
// } from "../controllers/requestController.js";
// const RequestRouter = Router();
// RequestRouter.post("/send/:id", sendRequest); // Send request to user
// RequestRouter.post("/accept/:id", acceptRequest); // user accepts the request send by one having :id
// RequestRouter.post("/reject/:id", rejectRequest); // Reject request from user
// RequestRouter.post("/cancel/:id", cancelRequest);
// RequestRouter.get("/incoming", viewIncoming);
// RequestRouter.get("/outgoing", getOutgoingRequests);
// export default RequestRouter;
